<%= form_with(model: time_slice, local: true) do |form| %>
  <% if time_slice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(time_slice.errors.count, "error") %> prohibited this time_slice from being saved:</h2>

      <ul>
        <% time_slice.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :up %>
    <%= form.number_field :up %>
  </div>

  <div class="field">
    <%= form.label :down %>
    <%= form.number_field :down %>
  </div>

  <div class="field">
    <%= form.label :counter_id %>
    <%= form.number_field :counter_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
